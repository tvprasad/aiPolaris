# backend.tf — Remote state in Azure Blob Storage.
# Uncomment and fill in real values before running `terraform init` for team use.
#
# terraform {
#   backend "azurerm" {
#     resource_group_name  = "PLACEHOLDER-tfstate-rg"
#     storage_account_name = "PLACEHOLDER-tfstate-sa"
#     container_name       = "tfstate"
#     key                  = "aipolaris.terraform.tfstate"
#   }
# }
